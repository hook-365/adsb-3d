import { describe, expect, it } from 'vitest';

// Phase 0 placeholder. Replaced by real unit tests starting in Phase 1
// (Aircraft normalization, feed parsing, etc.).
describe('phase 0 scaffold', () => {
  it('vitest is wired up', () => {
    expect(1 + 1).toBe(2);
  });
});
