// 'acars.*' UI strings. Populated by the i18n extraction; keys are flat and
// must carry the 'acars.' prefix (enforced by tests-unit/i18n.test.ts).
export const acarsStrings = {
  'acars.all_labels': 'All labels',
  'acars.no_text': '(no text)',
  'acars.no_messages_match': 'No messages match.',
} as const;
