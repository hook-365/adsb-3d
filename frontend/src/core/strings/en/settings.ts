// 'settings.*' UI strings. Populated by the i18n extraction; keys are flat and
// must carry the 'settings.' prefix (enforced by tests-unit/i18n.test.ts).
export const settingsStrings = {} as const;
