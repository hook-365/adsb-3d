// 'feeds.*' UI strings. Populated by the i18n extraction; keys are flat and
// must carry the 'feeds.' prefix (enforced by tests-unit/i18n.test.ts).
export const feedsStrings = {
  'feeds.select_feed': 'Select feed',
} as const;
