name: container-lint

# Advisory only: both jobs run continue-on-error so a lint finding never
# blocks a PR, but still shows up in the checks list for visibility.

on:
  push:
    branches: [main, master]
    paths:
      - 'entrypoint.sh'
      - 'docker-healthcheck.sh'
      - 'tests/integration/run.sh'
      - 'Dockerfile'
      - 'track-service/Dockerfile'
      - 'acars-service/Dockerfile'
      - '.github/workflows/container-lint.yml'
  pull_request:
    paths:
      - 'entrypoint.sh'
      - 'docker-healthcheck.sh'
      - 'tests/integration/run.sh'
      - 'Dockerfile'
      - 'track-service/Dockerfile'
      - 'acars-service/Dockerfile'
      - '.github/workflows/container-lint.yml'

permissions:
  contents: read

concurrency:
  group: container-lint-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: shellcheck
        run: shellcheck entrypoint.sh docker-healthcheck.sh tests/integration/run.sh

  hadolint:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        dockerfile:
          - Dockerfile
          - track-service/Dockerfile
          - acars-service/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - name: hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: ${{ matrix.dockerfile }}
