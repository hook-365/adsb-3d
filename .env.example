# =============================================================================
# ADS-B 3D Viewer - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
#
# Quick Start:
#   cp .env.example .env
#   nano .env  # Edit with your location and feeder URL
#   docker compose up -d
# =============================================================================

# =============================================================================
# REQUIRED: Station Location
# =============================================================================
# Your receiver's geographic coordinates
# Used for: distance calculations, range rings, UI display
#
# How to find:
#   - Google Maps: Right-click location → coordinates
#   - https://www.latlong.net/

LATITUDE=45.0000
LONGITUDE=-90.0000
ALTITUDE=1234
LOCATION_NAME=My Station

# =============================================================================
# REQUIRED: ADS-B Feeder URL
# =============================================================================
# URL or hostname of your ADS-B feeder (ultrafeeder, readsb, tar1090, etc.)
# Must expose the standard /data/aircraft.json endpoint
#
# Examples:
#   FEEDER_URL=http://ultrafeeder              # Docker service name
#   FEEDER_URL=http://192.168.1.50:8080        # External IP
#   FEEDER_URL=https://adsb.example.com        # External domain

FEEDER_URL=http://ultrafeeder

# =============================================================================
# OPTIONAL: Historical Mode
# =============================================================================
# Enable/disable historical track playback UI (default: true)
# true  = Show historical button, auto-detect Track API at /api/health
# false = Live-only mode (hides historical UI)
#
# Requires Track API service for historical features to work
# See README.md for full historical mode setup

ENABLE_HISTORICAL=true

# =============================================================================
# OPTIONAL: Timezone
# =============================================================================
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones

TZ=America/Chicago

# =============================================================================
# OPTIONAL: BASE_PATH for Reverse Proxy
# =============================================================================
# Leave blank for auto-detection (recommended)
# Set explicitly if deploying to a subdirectory:
#   BASE_PATH=/3d         # For example.com/3d
#   BASE_PATH=/adsb       # For example.com/adsb
#   BASE_PATH=            # For root domain

# BASE_PATH=
